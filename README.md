# Bash_CleanCookiesCache
SH script to clean browser cookies and cache from terminal.  
  
Removes cookies and cache in Firefox  
Removes all private data **and saved passwords** in Chrome  
  
Run this on startup!
